# Frontend overrides for backend endpoints / 백엔드 엔드포인트 재정의 설정
# Frontend runs on port 5174 by default / 프론트엔드는 기본적으로 5174 포트에서 실행됩니다
VITE_API_HTTP_BASE=http://localhost:8000/api
VITE_API_WS_BASE=ws://localhost:8000/api/ws
